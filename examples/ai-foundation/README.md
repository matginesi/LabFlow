# AI foundation examples

These files demonstrate the future LabFlow contracts for an immutable dataset snapshot, a model card, a reviewed prediction and a RAG evaluation set. They are inspectable examples only. The static POC does not train a model or connect to an LLM, vector database or remote service.
