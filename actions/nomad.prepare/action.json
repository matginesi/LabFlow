{
  "id": "nomad.prepare",
  "type": "DETERMINISTIC",
  "category": "nomad",
  "role": "researcher",
  "title": "Prepare NOMAD export",
  "short_title": "Prepare NOMAD export",
  "purpose": "Build and validate NOMAD staging deterministically from the current canonical Working Copy.",
  "strategy": "Build an explicit Canonical → NOMAD field map first, store it, then validate the generated schema/entry contract and package inputs. No AI decides readiness.",
  "output": "json",
  "mutation_scope": "",
  "steps": [
    {
      "id": "prepare",
      "type": "DETERMINISTIC",
      "fn": "nomad.build-plan",
      "capture_result": true
    },
    {
      "id": "store",
      "type": "DETERMINISTIC",
      "fn": "nomad.store-deterministic-plan"
    },
    {
      "id": "validate",
      "type": "DETERMINISTIC",
      "fn": "nomad.validate",
      "capture_result": true
    }
  ],
  "version": 1,
  "input_scope": "working-copy.canonical",
  "visibility": "public",
  "requires": []
}
