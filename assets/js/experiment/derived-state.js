(function(){
'use strict';
const LF=window.LabFlow=window.LabFlow||{};
const REGISTRY=[];
function removePath(root,path){const parts=String(path||'').split('.').filter(Boolean);if(!parts.length)return false;let node=root;for(let i=0;i<parts.length-1;i++){if(!node||typeof node!=='object')return false;node=node[parts[i]];}if(!node||typeof node!=='object'||!Object.prototype.hasOwnProperty.call(node,parts[parts.length-1]))return false;delete node[parts[parts.length-1]];return true;}
function register(id,spec){if(!id||!spec)throw new Error('DerivedState.register requires id and specification.');if(REGISTRY.some(function(x){return x.id===id;}))throw new Error('Duplicate derived projection: '+id);const row=Object.assign({id:id,dependsOn:[],paths:[],description:'',invalidate:null},spec);row.dependsOn=Array.isArray(row.dependsOn)?row.dependsOn.slice():[];row.paths=Array.isArray(row.paths)?row.paths.slice():[];REGISTRY.push(row);return row;}
function matches(row,scope){return row.dependsOn.includes('*')||row.dependsOn.includes(String(scope||'metadata'));}
function invalidate(exp,scope){if(!exp)return[];const changed=[];REGISTRY.forEach(function(row){if(!matches(row,scope))return;let touched=false;if(typeof row.invalidate==='function')touched=row.invalidate(exp,scope)!==false||touched;row.paths.forEach(function(path){touched=removePath(exp,path)||touched;});if(touched)changed.push(row.id);});return changed;}
function describe(){return REGISTRY.map(function(row){return{id:row.id,dependsOn:row.dependsOn.slice(),paths:row.paths.slice(),description:row.description};});}
register('canonical-index',{dependsOn:['dataset','analysis','design','metadata'],paths:['canonical'],description:'Canonical read index/evidence graph.'});
register('review-analysis',{dependsOn:['dataset'],paths:['datasetAnalysis'],description:'Deterministic review dossier and ambiguity selection.'});
register('analysis-summary',{dependsOn:['dataset','analysis'],paths:['analysisSummary','experimentBrief'],description:'Results summaries and AI interpretations derived from current scientific values.',invalidate:function(exp){if(!LF.ActionData)return false;const a=LF.ActionData.removeAnnotation(exp,'results.interpret');const b=LF.ActionData.removeAnnotation(exp,'results.compare');return a||b;}});
register('ambiguity-proposals',{dependsOn:['dataset'],paths:[],description:'AI ambiguity proposals tied to one dataset revision.',invalidate:function(exp){return LF.ActionData?LF.ActionData.removeProposal(exp,'dataset.resolve-ambiguities'):false;}});
register('design-analysis',{dependsOn:['dataset','design'],paths:['designAnalysis'],description:'Design projections/proposals tied to current experiment/design state.',invalidate:function(exp){if(!LF.ActionData)return false;const root=LF.ActionData.ensure(exp),had=Object.prototype.hasOwnProperty.call(root.proposals,'design.infer')||Object.prototype.hasOwnProperty.call(root.status,'design.infer');LF.ActionData.clear(exp,'design.infer');return had;}});
register('nomad-projection',{dependsOn:['dataset','analysis','validation','design','metadata'],description:'NOMAD mapping/validation state derived from the LabFlow Data.',invalidate:function(exp,scope){exp.nomad=exp.nomad||{};const had=!!(exp.nomad.validation||exp.nomad.mappingPlan||exp.nomad.staleSince||exp.nomad.upload);exp.nomad.validation=null;delete exp.nomad.mappingPlan;exp.nomad.staleSince=new Date().toISOString();exp.nomad.staleReason=scope;if(exp.nomad.upload){exp.nomad.upload.stale=true;exp.nomad.upload.staleReason=scope;}return had||true;}});
LF.DerivedState={register:register,invalidate:invalidate,describe:describe};
}());
