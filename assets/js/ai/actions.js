(function(){
'use strict';
const LF=window.LabFlow=window.LabFlow||{},Log=LF.Logger?LF.Logger.scope('actions'):null,AUTO_RETRY_DELAYS=[5000,10000];
let controller=null,running=false,failedRun=null;
function expOf(){return LF.State&&LF.State.state&&LF.State.state.experiment||null;}
function effective(id){return LF.Storage&&LF.Storage.getEffectiveAction?LF.Storage.getEffectiveAction(id):(LF.ActionRegistry&&LF.ActionRegistry.action?LF.ActionRegistry.action(id):null);}
function usesAi(def){return !!(def&&(def.steps||[]).some(function(s){return s.type==='AI';}));}
function cancel(){if(!controller||controller.signal.aborted)return false;controller.abort();return true;}
function actionError(code,message){const e=new Error(message);e.code=code;return e;}
function cancelError(){const e=actionError('ACTION_ABORTED','Action stopped by the user.');e.cancelled=true;return e;}
function classify(err){if(err&&err.cancelled)return err.code||'ACTION_ABORTED';if(err&&err.isContract)return err.code||'MODEL_OUTPUT_INVALID';if(err&&String(err.providerCode||'')==='1261')return'MODEL_CONTEXT_LENGTH';if(err&&err.status===429)return'MODEL_RATE_LIMIT';if(err&&err.timedOut)return'MODEL_TIMEOUT';if(err&&err.isNetwork)return'NETWORK_ERROR';return err&&err.code||'ACTION_FAILED';}
function getPath(root,path){return String(path||'').split('.').filter(Boolean).reduce(function(v,key){return v==null?undefined:v[key];},root);}
function parseJson(step,content){const parsed=LF.StructuredOutput.parse(content);if(!parsed.value){const e=LF.StructuredOutput.contractError(step.schema,null,{text:content,parseResult:parsed});e.modelOutput=content;throw e;}const normalized=step.schema&&LF.StructuredOutput.normalizeForSchema?LF.StructuredOutput.normalizeForSchema(step.schema,parsed.value):parsed.value;if(step.schema){const errors=LF.StructuredOutput.validate(step.schema,normalized,{registry:LF.ActionRegistry});if(errors.length){const e=LF.StructuredOutput.contractError(step.schema,normalized,{providerResponse:errors.join('\n')+'\n\n'+content});e.validationErrors=errors;e.modelOutput=content;throw e;}}return{value:normalized,note:parsed.strategy||'JSON'};}
function requestMeta(r){return{model:r.model,provider:r.provider,requestId:r.requestId,requestLogId:r.requestLogId,latencyMs:r.latencyMs,usage:r.usage,finishReason:r.finishReason,content:r.content,reasoning:r.reasoning};}
function safeRequest(spec){const headers=Object.assign({},spec.headers||{});if(headers.Authorization)headers.Authorization='Configured · redacted';return{method:'POST',endpoint:spec.url||'',headers:headers,body:spec.body||{}};}
function retryable(step,err){return step&&step.type==='AI'&&!(err&&err.cancelled);}
function feedbackFor(err){const d=err&&err.validationErrors&&err.validationErrors.join('\n')||err&&err.providerResponse||err&&err.message||'';return('Previous attempt failed with '+classify(err)+'.\n'+String(d).slice(0,6000)).trim();}
function delay(ms){return new Promise(function(resolve,reject){if(!controller||controller.signal.aborted){reject(cancelError());return;}const timer=setTimeout(done,ms);function clean(){clearTimeout(timer);controller&&controller.signal.removeEventListener('abort',aborted);}function done(){clean();resolve();}function aborted(){clean();reject(cancelError());}controller.signal.addEventListener('abort',aborted,{once:true});});}
function finishMutation(def){const scope=String(def&&def.mutation_scope||'');if(scope&&LF.State&&LF.State.touch)LF.State.touch(scope);}
function recordHistory(run,status,code){const e=expOf();if(!e)return;LF.State.ensureDerived(e);const entry=e.derived.actions[run.actionId]||{runs:[]};entry.lastStatus=status;entry.lastRunAt=new Date().toISOString();entry.runs.push({runId:run.runId,status:status,code:code||'',sourceRevision:run.sourceRevision,startedAt:run.startedAt,endedAt:new Date().toISOString(),steps:run.steps.slice(),attempts:run.attempts.slice(),work:run.work,result:run.result});if(entry.runs.length>12)entry.runs=entry.runs.slice(-12);e.derived.actions[run.actionId]=entry;}
async function runOne(run,step,opts,workItem,workIndex,workTotal){const e=expOf();if(step.type==='DETERMINISTIC'){const fn=LF.ActionSteps&&LF.ActionSteps[step.fn];if(!fn)throw actionError('ACTION_STEP_INVALID','Deterministic step is not registered: '+step.fn);return Promise.resolve(fn({actionId:run.actionId,exp:e,step:step,outputs:run.outputs,lastResult:run.result,params:run.params,selection:run.selection,sourceRevision:run.sourceRevision,requestMeta:run.requestMeta,workItem:workItem,workIndex:workIndex,workTotal:workTotal,progress:function(done,total,label){if(opts.onWork)opts.onWork({step:step.id,index:run.currentIndex,totalSteps:run.def.steps.length,workIndex:Number(done)||0,workTotal:Number(total)||0,label:label||''});}}));}if(step.type!=='AI')throw actionError('ACTION_STEP_INVALID','Unsupported action step type: '+step.type);const key=step.id+(workTotal>1?':'+workIndex:'');const context=LF.ActionContext.build(run.def,step,{outputs:run.outputs,userText:run.userText,params:run.params,selection:run.selection,retryFeedback:run.retryFeedback[key]||'',workItem:workItem,workIndex:workIndex,workTotal:workTotal});const jsonMode=step.output==='json',settings=LF.Storage.getAiSettings(),schema=jsonMode&&step.schema&&LF.ActionRegistry.schema?LF.ActionRegistry.schema(step.schema):null,assistantBudget=run.actionId==='assistant.chat'&&LF.Storage.getAssistantSettings?LF.Storage.getAssistantSettings().maxOutputTokens:null,outputBudget=Math.max(256,Number(assistantBudget||step.max_output_tokens||run.def.max_output_tokens||8192));const spec=LF.AI.buildRequest({messages:context.messageList,stream:settings.streaming!==false,maxTokens:outputBudget,timeoutMs:step.timeout_ms||null,hardTimeoutMs:step.deadline_ms||null,jsonMode:jsonMode,jsonSchema:schema,jsonSchemaName:step.schema||run.actionId,temperature:jsonMode?0.2:undefined});if(opts.onRequest)opts.onRequest({actionId:run.actionId,stepId:step.id,workIndex:workIndex,workTotal:workTotal,request:safeRequest(spec),budgetTokens:outputBudget});const response=await LF.AI.send(spec,{label:run.actionId+'.'+step.id+(workTotal>1?'.'+(workIndex+1):''),onProgress:function(p){if(opts.onProgress)opts.onProgress(Object.assign({},p,{stepId:step.id,workIndex:workIndex,workTotal:workTotal,workItem:workItem,budgetTokens:outputBudget}));}});run.requestMeta[key]=requestMeta(response);if(response.finishReason==='length'){const ex=actionError('MODEL_OUTPUT_TRUNCATED','The model reached its output limit before finishing this work unit.');ex.providerResponse=response.content;throw ex;}delete run.retryFeedback[key];if(jsonMode){const parsed=parseJson(step,response.content);run.parseNote=parsed.note;return parsed.value;}return String(response.content||'');}
async function runUnitWithRetry(run,step,opts,item,index,total){const key=step.id+(total>1?':'+index:'');let retryNo=0;while(true){if(controller&&controller.signal.aborted)throw cancelError();try{return await runOne(run,step,opts,item,index,total);}catch(err){const code=classify(err),can=retryNo<AUTO_RETRY_DELAYS.length&&retryable(step,err);run.attempts.push({step:step.id,index:run.currentIndex,workIndex:index,attempt:retryNo+1,status:can?'retry':'error',code:code,message:String(err&&err.message||err)});if(!can)throw err;const wait=AUTO_RETRY_DELAYS[retryNo];run.retryFeedback[key]=feedbackFor(err);retryNo++;if(opts.onAutoRetry)opts.onAutoRetry({actionId:run.actionId,step:step.id,index:run.currentIndex,total:run.def.steps.length,workIndex:index,workTotal:total,attempt:retryNo,maxAttempts:AUTO_RETRY_DELAYS.length,delayMs:wait,code:code,message:String(err&&err.message||err)});await delay(wait);}}}
function stepItem(step,status,index,total,note){return{id:step.id,type:step.type,status:status,index:index,total:total,note:note||''};}
function setStep(run,item,opts){run.steps=run.steps.filter(function(x){return x.index!==item.index;});run.steps.push(item);run.steps.sort(function(a,b){return a.index-b.index;});if(opts.onStep)opts.onStep(item);}
async function execute(run,startIndex,opts){for(let i=startIndex;i<run.def.steps.length;i++){const step=run.def.steps[i];run.currentIndex=i;run.failedIndex=null;run.failedWorkIndex=null;const items=step.foreach?getPath(run.outputs,step.foreach):null,units=step.foreach?(Array.isArray(items)?items:[]):[null];if(step.foreach&&!Array.isArray(items))throw actionError('ACTION_WORK_INVALID','Work-unit source is not an array: '+step.foreach);if(step.foreach&&!units.length){run.outputs[step.id]=[];setStep(run,stepItem(step,'done',i,run.def.steps.length,'0 work units'),opts);continue;}const start=step.foreach?Number(run.work[step.id]||0):0,results=step.foreach?(Array.isArray(run.outputs[step.id])?run.outputs[step.id]:[]):null;setStep(run,stepItem(step,'active',i,run.def.steps.length,step.foreach?('Work units '+start+'/'+units.length):step.type),opts);const started=performance.now();try{for(let u=start;u<units.length;u++){if(opts.onWork)opts.onWork({step:step.id,index:i,totalSteps:run.def.steps.length,workIndex:u,workTotal:units.length,label:units[u]&&units[u].label||units[u]&&units[u].id||''});const v=await runUnitWithRetry(run,step,opts,units[u],u,units.length);if(step.foreach){results[u]=v;run.outputs[step.id]=results;run.work[step.id]=u+1;run.result=results;}else{run.outputs[step.id]=v;if(v!==undefined&&(step.type==='AI'||run.def.type==='DETERMINISTIC'||step.capture_result))run.result=v;}}setStep(run,stepItem(step,'done',i,run.def.steps.length,step.foreach?(units.length+' work units · '+Math.round(performance.now()-started)+' ms'):(step.type==='AI'?(run.parseNote||''):(Math.round(performance.now()-started)+' ms'))),opts);}catch(err){run.failedIndex=i;run.failedWorkIndex=step.foreach?Number(run.work[step.id]||0):null;setStep(run,stepItem(step,'error',i,run.def.steps.length,String(err&&err.message||err)),opts);throw err;}}return run;}
function aiOutput(run){for(let i=(run.def.steps||[]).length-1;i>=0;i--){const step=run.def.steps[i];if(step.type==='AI'&&Object.prototype.hasOwnProperty.call(run.outputs,step.id))return run.outputs[step.id];}return null;}
function outcome(run,status,extra){return Object.assign({runId:run.runId,actionId:run.actionId,status:status,steps:run.steps,attempts:run.attempts,result:run.result,aiOutput:aiOutput(run),outputs:run.outputs,requestMeta:run.requestMeta,sourceRevision:run.sourceRevision,failedStep:run.failedIndex!=null&&run.def.steps[run.failedIndex]?run.def.steps[run.failedIndex].id:null,failedWorkIndex:run.failedWorkIndex,work:run.work},extra||{});}
async function startRun(run,startIndex,opts){running=true;controller=new AbortController();if(LF.AI&&LF.AI.acceptController)LF.AI.acceptController(controller);if(LF.State&&LF.State.startActionRun)LF.State.startActionRun({actionId:run.actionId,stepIndex:startIndex,runId:run.runId});try{await execute(run,startIndex,opts);running=false;failedRun=null;if(LF.AI&&LF.AI.acceptController)LF.AI.acceptController(null);if(LF.State&&LF.State.endActionRun)LF.State.endActionRun('done');finishMutation(run.def);recordHistory(run,'done');return outcome(run,'done');}catch(err){running=false;if(LF.AI&&LF.AI.acceptController)LF.AI.acceptController(null);const code=classify(err),status=err&&err.cancelled?'aborted':'error';if(LF.State&&LF.State.endActionRun)LF.State.endActionRun(status);failedRun=status==='error'?run:null;recordHistory(run,status,code);if(Log)Log.warn('action.failed',{runId:run.runId,actionId:run.actionId,step:run.failedIndex!=null&&run.def.steps[run.failedIndex]?run.def.steps[run.failedIndex].id:null,code:code,error:err});return outcome(run,status,{code:code,message:String(err&&err.message||'Action failed.'),error:err});}}
function newRun(actionId,def,opts){const exp=expOf();return{runId:(LF.Core&&LF.Core.uid?LF.Core.uid('action'):'action_'+Date.now()),actionId:actionId,def:def,sourceRevision:exp&&exp.sync&&exp.sync.revision||0,startedAt:new Date().toISOString(),params:opts.params||{},selection:opts.selection||null,userText:opts.userText||'',outputs:{},requestMeta:{},retryFeedback:{},steps:[],attempts:[],work:{},result:null,currentIndex:0,failedIndex:null,failedWorkIndex:null};}
function run(actionId,opts){opts=opts||{};const def=effective(actionId);if(!def)return Promise.resolve({actionId:actionId,status:'error',code:'ACTION_UNKNOWN',message:'Unknown action: '+actionId,steps:[]});if(running)return Promise.resolve({actionId:actionId,status:'error',code:'ACTION_BUSY',message:'Another action is already running.',steps:[]});const exp=expOf();if(!exp||!exp.id)return Promise.resolve({actionId:actionId,status:'error',code:'ACTION_UPLOAD_REQUIRED',message:'Upload a ZIP before running this action.',steps:[]});return startRun(newRun(actionId,def,opts),0,opts);}
function retry(opts){opts=opts||{};if(running)return Promise.resolve({status:'error',code:'ACTION_BUSY',message:'Another action is already running.',steps:[]});if(!failedRun)return Promise.resolve({status:'error',code:'ACTION_NO_FAILED_RUN',message:'There is no failed checkpoint to retry.',steps:[]});return startRun(failedRun,failedRun.failedIndex==null?0:failedRun.failedIndex,opts);}
LF.ActionRunner={run:run,retry:retry,cancel:cancel,isRunning:function(){return running;},failed:function(){return failedRun;},effective:effective,usesAi:usesAi,autoRetryDelays:AUTO_RETRY_DELAYS.slice()};
}());
