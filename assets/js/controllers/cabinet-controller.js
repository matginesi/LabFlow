(function(){
  'use strict';
  const LF=window.LabFlow=window.LabFlow||{};
  let searchTimer=null;
  function message(text,type){LF.UI.message(text,type||'info');}
  async function handleClick(e,ctx){
    const S=ctx.state,render=ctx.render;
    const select=e.target.closest('[data-cabinet-select]');if(select){S.state.ui.cabinetSelectedId=select.dataset.cabinetSelect;render();return true;}
    if(e.target.closest('#cabinetAddItem')){const el=document.getElementById('cabinetNewKind'),kind=el&&el.value||'solution',item=LF.Cabinet.create(kind,{});S.state.ui.cabinetKind=kind;S.state.ui.cabinetSelectedId=item.id;render();return true;}
    if(e.target.closest('#cabinetSaveCurrentSolution')){try{if(!ctx.hasExperiment())throw new Error('Open an experiment first.');const el=document.getElementById('cabinetCaptureSolution'),id=el&&el.value;if(!id)throw new Error('Choose a formulation to save.');const item=LF.Cabinet.saveDesignSolution(S.state.experiment,id);S.state.ui.cabinetKind='solution';S.state.ui.cabinetSelectedId=item.id;render();message('Formulation saved to Lab Cabinet.','success');}catch(err){message(err&&err.message||String(err),'error');}return true;}
    if(e.target.closest('#cabinetSaveCurrentStack')||e.target.closest('#cabinetSaveCurrentProtocol')){try{if(!ctx.hasExperiment())throw new Error('Open an experiment first.');const exp=ctx.ensureExperimentShape(S.state.experiment),dev=(exp.design.devices||[]).find(x=>String(x.id)===String(S.state.ui.selectedDesignDeviceId))||(exp.design.devices||[])[0];if(!dev)throw new Error('No Design experiment is available.');const protocol=!!e.target.closest('#cabinetSaveCurrentProtocol'),item=protocol?LF.Cabinet.saveDesignProtocol(exp,dev.id):LF.Cabinet.saveDesignStack(exp,dev.id);S.state.ui.cabinetKind=protocol?'protocol':'stack';S.state.ui.cabinetSelectedId=item.id;render();message(protocol?'Process recipe saved to Lab Cabinet.':'Device stack saved to Lab Cabinet.','success');}catch(err){message(err&&err.message||String(err),'error');}return true;}
    const duplicate=e.target.closest('[data-cabinet-duplicate]');if(duplicate){const item=LF.Cabinet.duplicate(duplicate.dataset.cabinetDuplicate);S.state.ui.cabinetKind=item.kind;S.state.ui.cabinetSelectedId=item.id;render();message('Cabinet resource duplicated.','success');return true;}
    if(e.target.closest('#cabinetExport')){const payload=LF.Cabinet.exportState(),blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='labflow-cabinet.json';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),0);message('Cabinet backup exported.','success');return true;}
    if(e.target.closest('#cabinetImport')){const input=document.getElementById('cabinetImportFile');if(input)input.click();return true;}
    const del=e.target.closest('[data-cabinet-delete]');if(del){const item=LF.Cabinet.get(del.dataset.cabinetDelete);if(!item)return true;const ok=await LF.UI.confirmAction('Delete “'+item.name+'” from Lab Cabinet? Existing experiments that used it keep their copied snapshots.',{title:'Delete Cabinet resource',confirmLabel:'Delete',cancelLabel:'Cancel',danger:true});if(ok){LF.Cabinet.remove(item.id);S.state.ui.cabinetSelectedId=null;render();message('Cabinet resource deleted. Existing experiment snapshots are unchanged.','success');}return true;}
    if(e.target.closest('#cabinetAddLayer')){const item=LF.Cabinet.get(S.state.ui.cabinetSelectedId);if(item&&item.kind==='stack'){const layers=(item.layers||[]).slice();layers.push({role:'',material:'',thickness:'',process:''});LF.Cabinet.update(item.id,{layers});render();}return true;}
    const remove=e.target.closest('[data-cabinet-remove-layer]');if(remove){const item=LF.Cabinet.get(S.state.ui.cabinetSelectedId);if(item&&item.kind==='stack'){const layers=(item.layers||[]).slice();layers.splice(Number(remove.dataset.cabinetRemoveLayer),1);LF.Cabinet.update(item.id,{layers});render();}return true;}
    const use=e.target.closest('[data-cabinet-use-design]');if(use){try{if(!ctx.hasExperiment()){message('Upload an experiment before using Cabinet resources in Design.','info');return true;}const exp=ctx.ensureExperimentShape(S.state.experiment),dev=(exp.design.devices||[]).find(x=>String(x.id)===String(S.state.ui.selectedDesignDeviceId))||(exp.design.devices||[])[0],item=LF.Cabinet.get(use.dataset.cabinetUseDesign);if(!dev||!item){message('No Design experiment is available.','warning');return true;}if(item.kind==='stack'&&(dev.stack||[]).length){const ok=await LF.UI.confirmAction('Replace the current stack for “'+(dev.name||'this experiment')+'” with “'+item.name+'”?',{title:'Use Cabinet stack',confirmLabel:'Replace stack',cancelLabel:'Cancel'});if(!ok)return true;}const out=LF.Cabinet.applyToDesign(exp,dev.id,item.id,{replace:true});if(out.changed){S.state.ui.selectedDesignDeviceId=dev.id;ctx.markModified('design');S.setRoute('experiment-design');message('Cabinet snapshot applied to '+(dev.name||'Design')+'.','success');}else message('The selected Cabinet resource adds no new Design values.','info');}catch(err){message('Cabinet resource could not be applied: '+(err&&err.message||String(err)),'error');}return true;}
    return false;
  }
  function handleChange(e,ctx){if(e.target.id!=='cabinetKindFilter')return false;ctx.state.state.ui.cabinetKind=e.target.value||'all';ctx.renderStable('.cabinet-library-tools');return true;}
  function handleInput(e,ctx){const S=ctx.state;if(e.target.id==='cabinetSearch'){S.state.ui.cabinetQuery=e.target.value;clearTimeout(searchTimer);searchTimer=setTimeout(function(){ctx.render();const input=document.getElementById('cabinetSearch');if(input){input.focus();input.setSelectionRange(input.value.length,input.value.length);}},160);return true;}const field=e.target.closest('[data-cabinet-field]');if(field){const item=LF.Cabinet.get(S.state.ui.cabinetSelectedId);if(item){const key=field.dataset.cabinetField,value=key==='tags'?field.value.split(',').map(x=>x.trim()).filter(Boolean):field.value,patch={};patch[key]=value;LF.Cabinet.update(item.id,patch);}return true;}const layerField=e.target.closest('[data-cabinet-layer-field]');if(layerField){const item=LF.Cabinet.get(S.state.ui.cabinetSelectedId);if(item&&item.kind==='stack'){const layers=(item.layers||[]).map(x=>Object.assign({},x)),layer=layers[Number(layerField.dataset.cabinetLayerIndex)];if(layer){layer[layerField.dataset.cabinetLayerField]=layerField.value;LF.Cabinet.update(item.id,{layers});}}return true;}return false;}
  async function handleFileChange(e,ctx){if(!e.target||e.target.id!=='cabinetImportFile')return false;const file=e.target.files&&e.target.files[0];if(!file)return true;try{const max=LF.Cabinet.limits?LF.Cabinet.limits().backupChars:2*1024*1024;if(file.size>max)throw new Error('Cabinet backup exceeds the '+Math.round(max/1024/1024)+' MB browser limit.');const text=await file.text(),out=LF.Cabinet.importState(text,'merge');ctx.state.state.ui.cabinetSelectedId=null;ctx.state.state.ui.cabinetKind='all';ctx.render();message('Imported '+out.imported+' Cabinet resource'+(out.imported===1?'':'s')+'.','success');}catch(err){message('Cabinet import failed: '+(err.message||String(err)),'error');}finally{e.target.value='';}return true;}
  LF.CabinetController={handleClick,handleChange,handleInput,handleFileChange};
})();
